const { createApp } = Vue

  createApp({
    data() {
      return {
        //variabili vue 3
        message: 'Hello Vue!',
      }
    },
    methods: {
        //funzioni vue 3
        
    }
  }).mount('#app')
